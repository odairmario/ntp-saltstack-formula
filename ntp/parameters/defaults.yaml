# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: ntp
  rootgroup: root
  config: '/etc/ntp'
  service:
    name: ntp
  subcomponent:
    config: '/etc/ntp-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
