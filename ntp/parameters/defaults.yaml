# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: ntp
  rootgroup: root
  config: /etc/ntp.conf
  pools:
    - "0.debian.pool.ntp.org iburst"
  service:
    name: ntp
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
